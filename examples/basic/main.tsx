import React from 'react';
import ReactDOM from 'react-dom/client';
import { BasicWakuConnection } from './BasicWakuConnection';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BasicWakuConnection />
  </React.StrictMode>,
);